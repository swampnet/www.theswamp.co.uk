# Stuff I don't understand and need to learn about

- I see nested .razor.css files. I assume these are css files that are scoped to the razor component with the same name? 
  So if I have a Chat.razor and a Chat.razor.css, the styles in Chat.razor.css will only apply to the Chat component? 
  That seems really useful for keeping styles organized and avoiding conflicts, but I need to understand how it works in more detail.


- Adding something like awsomefonts or some other js framework. Do I npm install that stuff? How does the build process work with that? 
  Do I need to configure something to make sure the js and css from those packages are included in the final build? 
  I need to understand how to integrate third-party js and css libraries into a blazor server app.

